# 鏁版嵁搴撹璁?
> PostgreSQL 涓轰富鏁版嵁搴擄紝鍏冩暟鎹拰搴旂敤鏁版嵁鍒嗙銆?
---

## 1. 鍏冩暟鎹瓨鍌ㄨ〃

ASTRAION 鐨勫厓鏁版嵁瀛樺偍鍦ㄤ互涓嬬郴缁熻〃涓細

### 1.1 astraion_model 鈥?妯″瀷瀹氫箟琛?
```sql
CREATE TABLE astraion_model (
    id              BIGSERIAL PRIMARY KEY,
    model_name      VARCHAR(128) NOT NULL UNIQUE,        -- 妯″瀷鏍囪瘑
    display_name    VARCHAR(256) NOT NULL,               -- 鏄剧ず鍚嶇О
    description     TEXT,                                 -- 鎻忚堪
    builtin         BOOLEAN NOT NULL DEFAULT FALSE,       -- 鏄惁鍐呯疆
    version         INTEGER NOT NULL DEFAULT 1,           -- 鐗堟湰鍙?    config          JSONB NOT NULL DEFAULT '{}',          -- 瀹屾暣閰嶇疆锛堝惈 fields, lifecycle, permissions, workflow, display锛?    status          VARCHAR(32) NOT NULL DEFAULT 'active',-- active | deprecated | disabled
    created_at      TIMESTAMP NOT NULL DEFAULT NOW(),
    updated_at      TIMESTAMP NOT NULL DEFAULT NOW(),
    created_by      BIGINT REFERENCES astraion_user(id)
);

COMMENT ON TABLE astraion_model IS 'ASTRAION 妯″瀷鍏冩暟鎹畾涔?;
```

### 1.2 astraion_model_field 鈥?妯″瀷瀛楁琛紙鍐椾綑锛屼究浜庢煡璇級

```sql
CREATE TABLE astraion_model_field (
    id              BIGSERIAL PRIMARY KEY,
    model_id        BIGINT NOT NULL REFERENCES astraion_model(id) ON DELETE CASCADE,
    field_name      VARCHAR(128) NOT NULL,               -- 瀛楁鍚?    field_type      VARCHAR(64) NOT NULL,                 -- 鏁版嵁绫诲瀷
    label           VARCHAR(256),                         -- 鏄剧ず鏍囩
    required        BOOLEAN NOT NULL DEFAULT FALSE,
    unique_flag     BOOLEAN NOT NULL DEFAULT FALSE,
    default_value   TEXT,
    max_length      INTEGER,
    min_length      INTEGER,
    field_order     INTEGER NOT NULL DEFAULT 0,
    options         JSONB,                                -- enum 閫夐」
    validation      JSONB,                                -- 鏍￠獙瑙勫垯
    target_model    VARCHAR(128),                         -- relation 绫诲瀷鐨勭洰鏍囨ā鍨?    readonly        BOOLEAN NOT NULL DEFAULT FALSE,
    hidden          BOOLEAN NOT NULL DEFAULT FALSE,
    description     TEXT,
    created_at      TIMESTAMP NOT NULL DEFAULT NOW()
);

COMMENT ON TABLE astraion_model_field IS '妯″瀷瀛楁瀹氫箟';
```

### 1.3 astraion_user 鈥?鐢ㄦ埛琛?
```sql
CREATE TABLE astraion_user (
    id              BIGSERIAL PRIMARY KEY,
    username        VARCHAR(128) NOT NULL UNIQUE,
    password_hash   VARCHAR(256) NOT NULL,               -- BCrypt 鍔犲瘑
    display_name    VARCHAR(256),
    email           VARCHAR(256),
    phone           VARCHAR(32),
    avatar          VARCHAR(512),
    role            VARCHAR(32) NOT NULL DEFAULT 'user',  -- root | admin | user
    dept_id         BIGINT,                               -- 閮ㄩ棬锛堝彲鎵╁睍锛?    extra_fields    JSONB DEFAULT '{}',                   -- 鑷畾涔夋墿灞曞瓧娈?    last_login_at   TIMESTAMP,
    created_at      TIMESTAMP NOT NULL DEFAULT NOW(),
    updated_at      TIMESTAMP NOT NULL DEFAULT NOW()
);

COMMENT ON TABLE astraion_user IS '绯荤粺鐢ㄦ埛琛?;
```

### 1.4 astraion_system_config 鈥?绯荤粺閰嶇疆琛紙浠?root 鍙锛?
```sql
CREATE TABLE astraion_system_config (
    id              BIGSERIAL PRIMARY KEY,
    config_key      VARCHAR(128) NOT NULL UNIQUE,
    config_value    TEXT NOT NULL,
    encrypted       BOOLEAN NOT NULL DEFAULT FALSE,       -- API Key 绛夊姞瀵嗗瓨鍌?    description     TEXT,
    updated_at      TIMESTAMP NOT NULL DEFAULT NOW()
);

-- 鍒濆鍖栨椂鍐欏叆锛?-- config_key = 'db.host', db.port, db.name, db.user, db.password (encrypted)
-- config_key = 'ai.provider', ai.model, ai.base_url, ai.api_key (encrypted)
-- config_key = 'ai.temperature', ai.max_tokens
-- config_key = 'system.initialized' 鈫?true/false

COMMENT ON TABLE astraion_system_config IS '绯荤粺閰嶇疆锛堜粎 ASTRAION root 鍙鍐欙級';
```

### 1.5 astraion_datasource 鈥?澶栭儴鏁版嵁婧愯〃

```sql
CREATE TABLE astraion_datasource (
    id              BIGSERIAL PRIMARY KEY,
    name            VARCHAR(128) NOT NULL,                -- 鏁版嵁婧愬悕绉?    db_type         VARCHAR(32) NOT NULL,                 -- postgresql | mysql | oracle | sqlserver
    host            VARCHAR(256) NOT NULL,
    port            INTEGER NOT NULL,
    database_name   VARCHAR(128) NOT NULL,
    username        VARCHAR(128) NOT NULL,
    password_enc    VARCHAR(512) NOT NULL,                -- 鍔犲瘑瀛樺偍
    mode            VARCHAR(16) NOT NULL DEFAULT 'readonly', -- readonly | readwrite
    status          VARCHAR(32) NOT NULL DEFAULT 'active',
    created_by      BIGINT REFERENCES astraion_user(id),
    created_at      TIMESTAMP NOT NULL DEFAULT NOW()
);

COMMENT ON TABLE astraion_datasource IS '澶栭儴鏁版嵁婧愰厤缃紙绠＄悊鍛樺彲杩藉姞锛屼富搴撲粎root鍙厤锛?;
```

### 1.6 astraion_workflow 鈥?娴佺▼瀹氫箟琛?
```sql
CREATE TABLE astraion_workflow (
    id              BIGSERIAL PRIMARY KEY,
    name            VARCHAR(256) NOT NULL,
    model_name      VARCHAR(128) NOT NULL,                -- 鍏宠仈鐨勪笟鍔℃ā鍨?    trigger         VARCHAR(64) NOT NULL,                 -- onCreate | onUpdate | manual
    config          JSONB NOT NULL,                       -- 瀹屾暣娴佺▼瀹氫箟锛坰teps, conditions锛?    status          VARCHAR(32) NOT NULL DEFAULT 'active',
    created_at      TIMESTAMP NOT NULL DEFAULT NOW(),
    updated_at      TIMESTAMP NOT NULL DEFAULT NOW()
);

COMMENT ON TABLE astraion_workflow IS '瀹℃壒娴佺▼瀹氫箟';
```

### 1.7 astraion_workflow_instance 鈥?娴佺▼杩愯瀹炰緥琛?
```sql
CREATE TABLE astraion_workflow_instance (
    id              BIGSERIAL PRIMARY KEY,
    workflow_id     BIGINT NOT NULL REFERENCES astraion_workflow(id),
    model_name      VARCHAR(128) NOT NULL,
    record_id       BIGINT NOT NULL,                      -- 鍏宠仈鐨勪笟鍔℃暟鎹?ID
    current_step    VARCHAR(128),                         -- 褰撳墠姝ラ ID
    status          VARCHAR(32) NOT NULL DEFAULT 'running',-- running | approved | rejected | cancelled
    initiator_id    BIGINT REFERENCES astraion_user(id),
    started_at      TIMESTAMP NOT NULL DEFAULT NOW(),
    finished_at     TIMESTAMP
);

COMMENT ON TABLE astraion_workflow_instance IS '娴佺▼杩愯瀹炰緥';
```

### 1.8 astraion_task 鈥?寰呭姙浠诲姟琛?
```sql
CREATE TABLE astraion_task (
    id              BIGSERIAL PRIMARY KEY,
    workflow_inst_id BIGINT REFERENCES astraion_workflow_instance(id),
    model_name      VARCHAR(128) NOT NULL,
    record_id       BIGINT NOT NULL,
    step_id         VARCHAR(128),
    title           VARCHAR(512) NOT NULL,
    assignee_id     BIGINT REFERENCES astraion_user(id),
    status          VARCHAR(32) NOT NULL DEFAULT 'pending',-- pending | completed | skipped | cancelled
    comment         TEXT,
    due_at          TIMESTAMP,
    completed_at    TIMESTAMP,
    created_at      TIMESTAMP NOT NULL DEFAULT NOW()
);

COMMENT ON TABLE astraion_task IS '寰呭姙浠诲姟';
```

### 1.9 astraion_document 鈥?鏂囨。/鏂囦欢琛?
```sql
CREATE TABLE astraion_document (
    id              BIGSERIAL PRIMARY KEY,
    file_name       VARCHAR(512) NOT NULL,
    file_url        VARCHAR(1024) NOT NULL,
    file_size       BIGINT,
    mime_type       VARCHAR(128),
    model_name      VARCHAR(128),                         -- 鍏宠仈鐨勪笟鍔℃ā鍨?    record_id       BIGINT,                               -- 鍏宠仈鐨勪笟鍔¤褰?ID
    field_name      VARCHAR(128),                         -- 鍏宠仈鐨勫瓧娈靛悕
    version         INTEGER NOT NULL DEFAULT 1,
    uploaded_by     BIGINT REFERENCES astraion_user(id),
    created_at      TIMESTAMP NOT NULL DEFAULT NOW()
);

COMMENT ON TABLE astraion_document IS '鏂囨。/鏂囦欢绠＄悊';
```

### 1.10 astraion_notification 鈥?閫氱煡琛?
```sql
CREATE TABLE astraion_notification (
    id              BIGSERIAL PRIMARY KEY,
    user_id         BIGINT NOT NULL REFERENCES astraion_user(id),
    title           VARCHAR(256) NOT NULL,
    content         TEXT,
    type            VARCHAR(64),                          -- task_assigned | workflow_update | system
    ref_model       VARCHAR(128),
    ref_record_id   BIGINT,
    read_flag       BOOLEAN NOT NULL DEFAULT FALSE,
    created_at      TIMESTAMP NOT NULL DEFAULT NOW()
);

COMMENT ON TABLE astraion_notification IS '绯荤粺閫氱煡';
```

### 1.11 astraion_audit_log 鈥?瀹¤鏃ュ織琛?
```sql
CREATE TABLE astraion_audit_log (
    id              BIGSERIAL PRIMARY KEY,
    user_id         BIGINT REFERENCES astraion_user(id),
    action          VARCHAR(64) NOT NULL,                 -- create | update | delete | login | export
    model_name      VARCHAR(128),
    record_id       BIGINT,
    changes         JSONB,                                -- 鍙樻洿鍐呭锛坥ld vs new锛?    ip_address      VARCHAR(64),
    user_agent      TEXT,
    created_at      TIMESTAMP NOT NULL DEFAULT NOW()
);

COMMENT ON TABLE astraion_audit_log IS '瀹¤鏃ュ織';
```

### 1.12 astraion_plugin 鈥?鎻掍欢娉ㄥ唽琛?
```sql
CREATE TABLE astraion_plugin (
    id              BIGSERIAL PRIMARY KEY,
    plugin_name     VARCHAR(128) NOT NULL UNIQUE,
    display_name    VARCHAR(256),
    version         VARCHAR(64),
    jar_path        VARCHAR(1024),                        -- 鎻掍欢 jar 鍖呰矾寰?    hook_points     JSONB NOT NULL DEFAULT '[]',          -- 娉ㄥ唽鐨勯挬瀛愮偣
    status          VARCHAR(32) NOT NULL DEFAULT 'active',
    config          JSONB DEFAULT '{}',
    created_at      TIMESTAMP NOT NULL DEFAULT NOW()
);

COMMENT ON TABLE astraion_plugin IS '鎻掍欢娉ㄥ唽琛?;
```

### 1.13 astraion_agent 鈥?鏅鸿兘浣撳畾涔夎〃锛坴2 棰勭暀锛?
```sql
CREATE TABLE astraion_agent (
    id              BIGSERIAL PRIMARY KEY,
    name            VARCHAR(256) NOT NULL,                -- 鏅鸿兘浣撳悕绉?    agent_code      VARCHAR(128) NOT NULL UNIQUE,         -- 鏅鸿兘浣撴爣璇?    model_override  VARCHAR(128),                         -- 鐙珛妯″瀷锛堟垨缁ф壙涓?AI锛?    system_prompt   TEXT NOT NULL,                        -- 绯荤粺鎻愮ず璇?    personality     VARCHAR(64),                          -- 鎬ф牸椋庢牸锛氫笓涓?浜插拰/涓ヨ皑/鍒涙剰
    expertise       JSONB DEFAULT '[]',                   -- 鎿呴暱棰嗗煙鏍囩
    tool_filter     JSONB DEFAULT '[]',                   -- 鍙敤鐨勫伐鍏风櫧鍚嶅崟
    knowledge_bases JSONB DEFAULT '[]',                   -- 缁戝畾鐨勭煡璇嗗簱 ID
    schedule_tasks  JSONB DEFAULT '[]',                   -- 瀹氭椂浠诲姟閰嶇疆
    auto_reply      BOOLEAN DEFAULT FALSE,                -- 鏄惁鑷姩鍝嶅簲
    collaboration   VARCHAR(64) DEFAULT 'standalone',     -- standalone | callable | cluster
    trigger_words   JSONB DEFAULT '[]',                   -- 瑙﹀彂鍏抽敭璇?    priority        INTEGER DEFAULT 5,                    -- 浼樺厛绾э紙1-10锛?    status          VARCHAR(32) DEFAULT 'inactive',       -- inactive | active | paused | archived
    created_by      BIGINT REFERENCES astraion_user(id),
    created_at      TIMESTAMP NOT NULL DEFAULT NOW(),
    updated_at      TIMESTAMP NOT NULL DEFAULT NOW()
);

COMMENT ON TABLE astraion_agent IS '鏅鸿兘浣撳畾涔夛紙v2 鍚敤锛?;
```

### 1.14 astraion_agent_session 鈥?鏅鸿兘浣撲細璇濊〃锛坴2 棰勭暀锛?
```sql
CREATE TABLE astraion_agent_session (
    id              BIGSERIAL PRIMARY KEY,
    agent_id        BIGINT NOT NULL REFERENCES astraion_agent(id),
    user_id         BIGINT REFERENCES astraion_user(id),
    title           VARCHAR(512),
    status          VARCHAR(32) DEFAULT 'active',
    created_at      TIMESTAMP NOT NULL DEFAULT NOW()
);

COMMENT ON TABLE astraion_agent_session IS '鏅鸿兘浣撲細璇濊褰曪紙v2 鍚敤锛?;
```

### 1.15 astraion_agent_message 鈥?鏅鸿兘浣撻棿閫氫俊琛紙v2 棰勭暀锛?
```sql
CREATE TABLE astraion_agent_message (
    id              BIGSERIAL PRIMARY KEY,
    from_agent_id   BIGINT REFERENCES astraion_agent(id),
    to_agent_id     BIGINT REFERENCES astraion_agent(id),
    content         TEXT NOT NULL,
    message_type    VARCHAR(64),                          -- task | query | response | notify
    task_id         VARCHAR(128),                         -- 鍏宠仈浠诲姟
    created_at      TIMESTAMP NOT NULL DEFAULT NOW()
);

COMMENT ON TABLE astraion_agent_message IS '鏅鸿兘浣撻棿閫氫俊璁板綍锛坴2 鍚敤锛?;
```

---

## 2. 涓氬姟鏁版嵁瀛樺偍

### 2.1 鍔ㄦ€佽〃鍛藉悕瑙勫垯

```
astraion_data_{model_name}
```

绀轰緥锛?```sql
-- 绠＄悊鍛樺垱寤?customer 妯″瀷鍚庯紝寮曟搸鑷姩鎵ц锛?CREATE TABLE astraion_data_customer (
    id              BIGSERIAL PRIMARY KEY,
    _created_at     TIMESTAMP NOT NULL DEFAULT NOW(),
    _updated_at     TIMESTAMP NOT NULL DEFAULT NOW(),
    _created_by     BIGINT,
    _updated_by     BIGINT,
    _version        INTEGER NOT NULL DEFAULT 1,
    
    name            VARCHAR(200),
    phone           VARCHAR(32),
    email           VARCHAR(256),
    dept_id         BIGINT,
    status          VARCHAR(32) DEFAULT 'active',
    -- 鍏朵粬瀛楁鏍规嵁妯″瀷瀹氫箟鍔ㄦ€佺敓鎴?);
```

### 2.2 绯荤粺鑷姩瀛楁

姣忎釜鍔ㄦ€佽〃閮藉寘鍚互涓嬬郴缁熷瓧娈碉細

| 瀛楁鍚?| 绫诲瀷 | 璇存槑 |
|--------|------|------|
| id | BIGSERIAL | 涓婚敭 |
| _created_at | TIMESTAMP | 鍒涘缓鏃堕棿 |
| _updated_at | TIMESTAMP | 鏇存柊鏃堕棿 |
| _created_by | BIGINT | 鍒涘缓鑰?|
| _updated_by | BIGINT | 鏇存柊鑰?|
| _version | INTEGER | 涔愯閿佺増鏈?|

---

## 3. 鏁版嵁搴撳垵濮嬪寲

### 3.1 绯荤粺鍐呯疆鏁版嵁

```sql
-- 鍒涘缓鏁版嵁搴?CREATE DATABASE astraion WITH ENCODING 'UTF8';

-- 鍒涘缓鎵╁睍
CREATE EXTENSION IF NOT EXISTS "pgcrypto";   -- 鐢ㄤ簬鍔犲瘑
CREATE EXTENSION IF NOT EXISTS "uuid-ossp";  -- 鐢ㄤ簬 UUID

-- ASTRAION root 鐢ㄦ埛锛堥娆＄櫥褰曠敤锛?INSERT INTO astraion_user (username, password_hash, display_name, role, status)
VALUES ('ASTRAION', '$2a$10$BUILT_IN_ROOT_PASSWORD_HASH', 'ASTRAION Root', 'root', 'active');
-- 娉ㄦ剰锛歳oot 鐢ㄦ埛瀵嗙爜灏嗗湪棣栨鐧诲綍鍚庤姹備慨鏀?
-- 鍒濆鍖栫郴缁熼厤缃褰曪紙寰?root 濉厖锛?INSERT INTO astraion_system_config (config_key, config_value, encrypted) VALUES
('system.initialized', 'false', false),
('db.host', '', false),
('db.port', '5432', false),
('db.name', '', false),
('db.user', '', false),
('db.password', '', true),
('ai.provider', '', false),
('ai.model', '', false),
('ai.base_url', '', false),
('ai.api_key', '', true),
('ai.temperature', '0.7', false),
('ai.max_tokens', '4096', false);
```

---

## 4. 琛ㄥ叧绯诲浘

```
astraion_system_config锛堢嫭绔嬶紝浠?root 鍙锛?
astraion_model
    鈹溾攢鈹€ astraion_model_field (涓€瀵瑰)
    鈹?    鈹溾攢鈹€ astraion_workflow (涓€瀵瑰)
    鈹?  鈹斺攢鈹€ astraion_workflow_instance (涓€瀵瑰)
    鈹?      鈹斺攢鈹€ astraion_task (涓€瀵瑰)
    鈹?    鈹溾攢鈹€ astraion_document (涓€瀵瑰)
    鈹?    鈹斺攢鈹€ astraion_data_{model_name} (鍔ㄦ€佽〃锛屽搴旀瘡涓ā鍨?

astraion_user
    鈹溾攢鈹€ astraion_task (涓€瀵瑰锛屼綔涓?assignee)
    鈹溾攢鈹€ astraion_notification (涓€瀵瑰)
    鈹溾攢鈹€ astraion_audit_log (涓€瀵瑰)
    鈹斺攢鈹€ astraion_workflow_instance (涓€瀵瑰锛屼綔涓?initiator)

astraion_datasource锛堝閮ㄦ暟鎹簮锛岀鐞嗗憳鍙拷鍔狅級

astraion_plugin锛堢嫭绔嬶級

astraion_document锛堢嫭绔嬶紝閫氳繃 model_name + record_id 鍏宠仈鍒颁换鎰忔ā鍨嬶級

astraion_agent锛坴2 棰勭暀锛?    鈹斺攢鈹€ astraion_agent_session (涓€瀵瑰)
    鈹斺攢鈹€ astraion_agent_message (鍙戦€?鎺ユ敹)

astraion_agent_message
    鈹溾攢鈹€ from_agent 鈫?astraion_agent
    鈹斺攢鈹€ to_agent 鈫?astraion_agent
```

---

## 5. 澶氭暟鎹簮鏋舵瀯

```
                    ASTRAION 寮曟搸
                         鈹?            鈹屸攢鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹尖攢鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹?            鈹?           鈹?           鈹?            鈻?           鈻?           鈻?     鈹屸攢鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹?鈹屸攢鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹?鈹屸攢鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹?     鈹備富鏁版嵁婧?  鈹?鈹傛棫CRM     鈹?鈹傛棫ERP     鈹?     鈹侾ostgreSQL鈹?鈹侻ySQL     鈹?鈹侽racle    鈹?     鈹?         鈹?鈹?鍙)    鈹?鈹?鍙)    鈹?     鈹俒root     鈹?鈹俒admin    鈹?鈹俒admin    鈹?     鈹?鐙韩閰嶇疆] 鈹?鈹?鍙拷鍔燷  鈹?鈹?鍙拷鍔燷  鈹?     鈹斺攢鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹?鈹斺攢鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹?鈹斺攢鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹?
涓绘暟鎹簮閰嶇疆锛?  - 浠?ASTRAION root 鍙厤缃?  - 瀛樺偍鎵€鏈夊厓鏁版嵁鍜屼笟鍔℃暟鎹?  - 涓嶅彲鍒犻櫎

澶栭儴鏁版嵁婧愶細
  - 绠＄悊鍛樺彲閫氳繃瀵硅瘽杩藉姞
  - 鏀寔閫嗗悜宸ョ▼锛氳嚜鍔ㄨ鍙栬〃缁撴瀯 鈫?鐢熸垚妯″瀷鍏冩暟鎹?  - 鏀寔鍙妯″紡锛堟帹鑽愶級鍜岃鍐欐ā寮?  - 澶栭儴鏁版嵁鍙 ASTRAION 鏉冮檺妯″瀷瑕嗙洊绠＄悊
```
