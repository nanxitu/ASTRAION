# 鏍稿績寮曟搸璁捐

> ASTRAION 杩愯鏃跺紩鎿庣殑鏍稿績妯″潡璁捐銆?
---

## 1. 寮曟搸鎬讳綋缁撴瀯

```
鈹屸攢鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹?鈹?                  ASTRAION Engine                      鈹?鈹?                                                      鈹?鈹? 鈹屸攢鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹? 鈹?鈹? 鈹?          MetadataEngine                       鈹? 鈹?鈹? 鈹? 绠＄悊鎵€鏈夋ā鍨嬪畾涔夈€佸瓧娈靛畾涔夈€佹ā鍨嬮棿鍏崇郴          鈹? 鈹?鈹? 鈹斺攢鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹? 鈹?鈹?                                                      鈹?鈹? 鈹屸攢鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹?鈹屸攢鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹?         鈹?鈹? 鈹? DynamicCRUD      鈹?鈹? DynamicQuery     鈹?         鈹?鈹? 鈹? 鍩轰簬鍏冩暟鎹殑閫氱敤  鈹?鈹? 閫氱敤鏌ヨ寮曟搸      鈹?         鈹?鈹? 鈹? 澧炲垹鏀规煡         鈹?鈹? 鏀寔绛涢€?鎺掑簭/鍒嗛〉 鈹?         鈹?鈹? 鈹斺攢鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹?鈹斺攢鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹?         鈹?鈹?                                                      鈹?鈹? 鈹屸攢鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹?鈹屸攢鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹?         鈹?鈹? 鈹? PermissionEngine 鈹?鈹? WorkflowEngine   鈹?         鈹?鈹? 鈹? 鏉冮檺琛ㄨ揪寮忚В鏋?   鈹?鈹? 瀹℃壒娴佺▼椹卞姩      鈹?         鈹?鈹? 鈹? 琛岀骇/瀛楁绾ф潈闄? 鈹?鈹? 姝ラ娴佽浆/寰呭姙绠＄悊  鈹?         鈹?鈹? 鈹斺攢鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹?鈹斺攢鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹?         鈹?鈹?                                                      鈹?鈹? 鈹屸攢鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹?鈹屸攢鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹?         鈹?鈹? 鈹? PluginEngine     鈹?鈹? RuleEngine       鈹?         鈹?鈹? 鈹? 鎻掍欢鐑姞杞?       鈹?鈹? 琛ㄨ揪寮忚В鏋愭墽琛?   鈹?         鈹?鈹? 鈹? 鐢熷懡鍛ㄦ湡閽╁瓙绠＄悊  鈹?鈹? 瑙勫垯璁＄畻         鈹?         鈹?鈹? 鈹斺攢鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹?鈹斺攢鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹?         鈹?鈹?                                                      鈹?鈹? 鈹屸攢鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹? 鈹?鈹? 鈹?          DynamicTableManager                   鈹? 鈹?鈹? 鈹? 鍔ㄦ€佸缓琛ㄣ€丄LTER TABLE銆佸悓姝ユā鍨嬪瓧娈靛彉鏇?        鈹? 鈹?鈹? 鈹斺攢鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹? 鈹?鈹斺攢鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹?```

---

## 2. 鍏冩暟鎹紩鎿?(MetadataEngine)

璐熻矗妯″瀷瀹氫箟鐨勫叏鐢熷懡鍛ㄦ湡绠＄悊銆?
```
createModel(modelMeta)  鈫?瀛樺偍鍏冩暟鎹?鈫?瑙﹀彂寤鸿〃 鈫?娉ㄥ唽API 鈫?娉ㄥ唽宸ュ叿
getModel(modelName)     鈫?璇诲彇鍏冩暟鎹紙鍚瓧娈?鍏崇郴/鏉冮檺绛夛級
updateModel(modelName, changes) 鈫?鏇存柊鍏冩暟鎹?鈫?鍚屾琛ㄧ粨鏋?deleteModel(modelName)  鈫?鍒犻櫎鍏冩暟鎹?鈫?鍒犺〃 鈫?娉ㄩ攢API
listModels()            鈫?鍒楀嚭鎵€鏈夋ā鍨?getModelFields(modelName) 鈫?鑾峰彇瀛楁瀹氫箟
```

**绫昏璁★細**

```java
@Component
public class MetadataEngine {
    
    private final JdbcTemplate jdbcTemplate;
    private final DynamicTableManager tableManager;
    
    // 妯″瀷缂撳瓨锛堟湰鍦扮紦瀛橈紝瀹氭湡鍒锋柊锛?    private final Map<String, ModelMeta> modelCache = new ConcurrentHashMap<>();
    
    public ModelMeta createModel(ModelMeta meta) {
        // 1. 鏍￠獙瀛楁瀹氫箟
        validateFields(meta.getFields());
        // 2. 瀛樺偍鍏冩暟鎹埌 astraion_model 琛?        saveModelMeta(meta);
        // 3. 鍔ㄦ€佸缓琛?        tableManager.createTable(meta);
        // 4. 鍐欏叆缂撳瓨
        modelCache.put(meta.getModelName(), meta);
        // 5. 杩斿洖缁撴灉
        return meta;
    }
    
    public ModelMeta getModel(String modelName) {
        // 浼樺厛浠庣紦瀛樺彇
        return modelCache.computeIfAbsent(modelName, this::loadModelFromDb);
    }
    
    public void addFields(String modelName, List<FieldDef> newFields) {
        ModelMeta model = getModel(modelName);
        // 1. 鏍￠獙鏂板瓧娈?        validateFields(newFields);
        // 2. ALTER TABLE 娣诲姞鍒?        tableManager.addColumns(model, newFields);
        // 3. 鏇存柊鍏冩暟鎹?        model.getFields().addAll(newFields);
        saveModelMeta(model);
        // 4. 鏇存柊缂撳瓨
    }
}
```

---

## 3. 鍔ㄦ€?CRUD 寮曟搸 (DynamicCRUD)

鍩轰簬鍏冩暟鎹嚜鍔ㄧ敓鎴愬鍒犳敼鏌ラ€昏緫锛屼笉渚濊禆浠讳綍棰勫埗 Entity銆?
```java
@Component
public class DynamicCRUD {
    
    private final MetadataEngine metadataEngine;
    private final JdbcTemplate jdbcTemplate;
    private final PermissionEngine permissionEngine;
    private final PluginEngine pluginEngine;
    
    // 鍒涘缓璁板綍
    public DataResult create(String modelName, Map<String, Object> data, UserContext ctx) {
        ModelMeta model = metadataEngine.getModel(modelName);
        
        // 1. 鏉冮檺鏍￠獙
        permissionEngine.check(model, "create", data, ctx);
        
        // 2. 鎵ц beforeCreate 鎻掍欢
        pluginEngine.executeHooks(model, "beforeCreate", data, ctx);
        
        // 3. 瀛楁鏍￠獙锛堝繀濉€佹牸寮忋€佸敮涓€鎬э級
        validateFields(model, data);
        
        // 4. 缁勮 SQL 骞舵墽琛?        String sql = buildInsertSQL(model, data);
        Long id = jdbcTemplate.queryForObject(sql, Long.class);
        
        // 5. 鎵ц afterCreate 鎻掍欢
        data.put("id", id);
        pluginEngine.executeHooks(model, "afterCreate", data, ctx);
        
        return DataResult.success(id);
    }
    
    // 鏌ヨ鍒楄〃
    public PageResult queryList(String modelName, QueryParams params, UserContext ctx) {
        ModelMeta model = metadataEngine.getModel(modelName);
        
        // 1. 鏉冮檺鏍￠獙 + 鑷姩杩藉姞琛岀骇杩囨护
        permissionEngine.applyRowFilter(model, "read", params, ctx);
        
        // 2. 鏋勫缓鍔ㄦ€?SQL
        String countSql = buildCountSQL(model, params);
        String querySql = buildQuerySQL(model, params);
        
        // 3. 鎵ц鏌ヨ
        long total = jdbcTemplate.queryForObject(countSql, Long.class);
        List<Map<String, Object>> items = jdbcTemplate.queryForList(querySql);
        
        return PageResult.of(total, items, params);
    }
    
    // 鏋勫缓 INSERT SQL锛堟牴鎹厓鏁版嵁鍔ㄦ€佹嫾锛?    private String buildInsertSQL(ModelMeta model, Map<String, Object> data) {
        StringBuilder sql = new StringBuilder();
        sql.append("INSERT INTO astraion_data_").append(model.getModelName());
        sql.append(" (");
        
        List<String> columns = new ArrayList<>();
        List<String> placeholders = new ArrayList<>();
        
        for (FieldDef field : model.getFields()) {
            if (data.containsKey(field.getName())) {
                columns.add(field.getName());
                placeholders.add("?");
            }
        }
        
        sql.append(String.join(", ", columns));
        sql.append(") VALUES (");
        sql.append(String.join(", ", placeholders));
        sql.append(") RETURNING id");
        
        return sql.toString();
    }
}
```

---

## 4. 鏉冮檺寮曟搸 (PermissionEngine)

瑙ｆ瀽鍜屾墽琛屾潈闄愯鍒欍€?
```java
@Component
public class PermissionEngine {
    
    private final AviatorEvaluator evaluator;
    
    public void check(ModelMeta model, String action, Object data, UserContext ctx) {
        PermissionDef perm = model.getPermissions();
        if (perm == null) return;
        
        // 1. 妫€鏌ユ搷浣滅骇鍒潈闄?        String rule = perm.getRule(action);
        if (rule != null && !evaluate(rule, ctx)) {
            throw new PermissionException("鏃犳潈鎵ц姝ゆ搷浣?);
        }
        
        // 2. 妫€鏌ヨ绾ф潈闄?        if (data instanceof Map) {
            for (RowPermission rp : perm.getRowPermissions()) {
                if (rp.getActions().contains(action)) {
                    Map<String, Object> env = buildEnv(ctx, (Map) data);
                    if (!evaluator.execute(rp.getRule(), env)) {
                        throw new PermissionException("鏃犳潈鎿嶄綔姝ゆ暟鎹?);
                    }
                }
            }
        }
    }
    
    // 鑷姩杩藉姞琛岀骇杩囨护鏉′欢
    public void applyRowFilter(ModelMeta model, String action, QueryParams params, UserContext ctx) {
        PermissionDef perm = model.getPermissions();
        if (perm == null) return;
        
        for (RowPermission rp : perm.getRowPermissions()) {
            if (rp.getActions().contains("read")) {
                // 灏嗚绾ц鍒欒浆涓?SQL 鏉′欢杩藉姞鍒版煡璇?                String condition = convertToSql(rp.getRule(), ctx);
                params.addFilter(condition);
            }
        }
    }
}
```

---

## 5. 娴佺▼寮曟搸 (WorkflowEngine)

椹卞姩瀹℃壒娴佺▼鐨勬祦杞€?
```java
@Component
public class WorkflowEngine {
    
    public void startWorkflow(String modelName, Long recordId, UserContext ctx) {
        // 1. 鏌ユ壘璇ユā鍨嬪叧鑱旂殑娴佺▼瀹氫箟
        WorkflowDef workflow = workflowRepo.findByModel(modelName);
        
        // 2. 鍒涘缓娴佺▼瀹炰緥
        WorkflowInstance inst = new WorkflowInstance();
        inst.setWorkflowId(workflow.getId());
        inst.setModelName(modelName);
        inst.setRecordId(recordId);
        inst.setCurrentStep(workflow.getFirstStep().getId());
        inst.setStatus("running");
        inst.setInitiatorId(ctx.getUserId());
        workflowInstanceRepo.save(inst);
        
        // 3. 鍒涘缓绗竴涓緟鍔炰换鍔?        createTask(inst, workflow.getFirstStep(), ctx);
        
        // 4. 鍙戦€侀€氱煡
        notificationService.notifyAssignee(inst, workflow.getFirstStep());
    }
    
    public void completeTask(Long taskId, String action, String comment, UserContext ctx) {
        // 1. 鑾峰彇浠诲姟鍜屾祦绋嬪疄渚?        Task task = taskRepo.findById(taskId);
        WorkflowInstance inst = workflowInstanceRepo.findById(task.getWorkflowInstanceId());
        WorkflowDef workflow = workflowRepo.findById(inst.getWorkflowId());
        
        // 2. 鑾峰彇褰撳墠姝ラ瀹氫箟
        StepDef currentStep = workflow.getStep(inst.getCurrentStep());
        
        // 3. 鏍规嵁 action 鎵惧埌涓嬩竴姝?        String nextStepId = currentStep.getNextStep(action);
        
        if ("end".equals(nextStepId)) {
            // 娴佺▼缁撴潫
            inst.setStatus("approved");
            inst.setFinishedAt(LocalDateTime.now());
            workflowInstanceRepo.save(inst);
        } else if (isRejectAndReturnToStart(nextStepId, workflow)) {
            // 椹冲洖鑷抽噸鏂版彁浜?            inst.setCurrentStep(nextStepId);
            inst.setStatus("rejected");
            workflowInstanceRepo.save(inst);
            createTask(inst, workflow.getStep(nextStepId), ctx);
        } else {
            // 杩涘叆涓嬩竴姝?            inst.setCurrentStep(nextStepId);
            workflowInstanceRepo.save(inst);
            createTask(inst, workflow.getStep(nextStepId), ctx);
        }
        
        // 4. 瀹屾垚浠诲姟
        task.setStatus("completed");
        task.setComment(comment);
        task.setCompletedAt(LocalDateTime.now());
        taskRepo.save(task);
    }
}
```

---

## 6. 鎻掍欢寮曟搸 (PluginEngine)

鏀寔鐑姞杞界殑涓氬姟閫昏緫鎻掍欢銆?
```java
// 鎻掍欢鎺ュ彛
public interface AstraionPlugin {
    String getName();
    String getVersion();
    List<String> getHookPoints();     // 濡?["afterCreate:order", "beforeUpdate:customer"]
    PluginResult execute(PluginContext ctx);
}

// 鎻掍欢涓婁笅鏂?public class PluginContext {
    private String modelName;
    private String hookPoint;          // beforeCreate / afterCreate / etc.
    private Map<String, Object> data;  // 褰撳墠鎿嶄綔鐨勬暟鎹?    private Map<String, Object> oldData; // 鏇存柊鍓嶇殑鏁版嵁锛堜粎 update锛?    private UserContext user;
}

// 鎻掍欢寮曟搸鏍稿績
@Component
public class PluginEngine {
    
    private final Map<String, List<AstraionPlugin>> hookRegistry = new ConcurrentHashMap<>();
    private final PluginClassLoader classLoader = new PluginClassLoader();
    
    // 鍔犺浇鎻掍欢 jar
    public void loadPlugin(Path jarPath) {
        AstraionPlugin plugin = classLoader.loadPlugin(jarPath);
        for (String hook : plugin.getHookPoints()) {
            hookRegistry.computeIfAbsent(hook, k -> new CopyOnWriteArrayList<>()).add(plugin);
        }
    }
    
    // 鎵ц鎸囧畾閽╁瓙鐨勬墍鏈夋彃浠?    public void executeHooks(ModelMeta model, String hookPoint, Map<String, Object> data, UserContext ctx) {
        String key = hookPoint + ":" + model.getModelName();
        List<AstraionPlugin> plugins = hookRegistry.get(key);
        if (plugins == null) return;
        
        PluginContext context = new PluginContext();
        context.setModelName(model.getModelName());
        context.setHookPoint(hookPoint);
        context.setData(data);
        context.setUser(ctx);
        
        for (AstraionPlugin plugin : plugins) {
            PluginResult result = plugin.execute(context);
            if (!result.isSuccess()) {
                throw new PluginException(result.getErrorMessage());
            }
        }
    }
    
    // 鑷畾涔?ClassLoader锛屾敮鎸佺儹鍔犺浇
    static class PluginClassLoader extends URLClassLoader {
        public PluginClassLoader() {
            super(new URL[0], AstraionPlugin.class.getClassLoader());
        }
        
        public AstraionPlugin loadPlugin(Path jarPath) throws Exception {
            addURL(jarPath.toUri().toURL());
            // 鎵弿 jar 鍖呬腑鐨?AstraionPlugin 瀹炵幇绫?            // 閫氳繃 ServiceLoader 鎴?娉ㄨВ鎵弿
            ServiceLoader<AstraionPlugin> loader = ServiceLoader.load(AstraionPlugin.class, this);
            return loader.iterator().next();
        }
    }
}
```

---

## 7. 鍔ㄦ€佽〃绠＄悊鍣?(DynamicTableManager)

```java
@Component
public class DynamicTableManager {
    
    private final JdbcTemplate jdbcTemplate;
    
    private static final String TABLE_PREFIX = "astraion_data_";
    
    public void createTable(ModelMeta model) {
        StringBuilder sql = new StringBuilder();
        sql.append("CREATE TABLE ").append(TABLE_PREFIX).append(model.getModelName());
        sql.append(" (");
        sql.append("id BIGSERIAL PRIMARY KEY, ");
        sql.append("_created_at TIMESTAMP NOT NULL DEFAULT NOW(), ");
        sql.append("_updated_at TIMESTAMP NOT NULL DEFAULT NOW(), ");
        sql.append("_created_by BIGINT, ");
        sql.append("_updated_by BIGINT, ");
        sql.append("_version INTEGER NOT NULL DEFAULT 1, ");
        
        for (FieldDef field : model.getFields()) {
            sql.append(field.getName()).append(" ");
            sql.append(mapToSqlType(field));
            if (field.isRequired()) sql.append(" NOT NULL");
            if (field.getDefaultValue() != null) sql.append(" DEFAULT ").append(field.getDefaultValue());
            sql.append(", ");
        }
        
        sql.append("UNIQUE(id)");
        sql.append(")");
        
        jdbcTemplate.execute(sql.toString());
        
        // 鍒涘缓绱㈠紩
        for (IndexDef index : model.getIndexes()) {
            String idxSql = "CREATE INDEX idx_" + model.getModelName() + "_" + index.getName()
                + " ON " + TABLE_PREFIX + model.getModelName() + " (" + index.getColumns() + ")";
            jdbcTemplate.execute(idxSql);
        }
    }
    
    public void addColumns(ModelMeta model, List<FieldDef> newFields) {
        for (FieldDef field : newFields) {
            String sql = "ALTER TABLE " + TABLE_PREFIX + model.getModelName()
                + " ADD COLUMN " + field.getName() + " " + mapToSqlType(field);
            jdbcTemplate.execute(sql);
        }
    }
    
    private String mapToSqlType(FieldDef field) {
        return switch (field.getType()) {
            case "string" -> "VARCHAR(" + (field.getMaxLength() != null ? field.getMaxLength() : 255) + ")";
            case "text" -> "TEXT";
            case "integer" -> "INTEGER";
            case "decimal" -> "DECIMAL(18,6)";
            case "boolean" -> "BOOLEAN";
            case "date" -> "DATE";
            case "datetime" -> "TIMESTAMP";
            case "enum", "enumMulti", "json", "relationMulti" -> "JSONB";
            case "encrypted", "password" -> "VARCHAR(512)";
            default -> "VARCHAR(255)";
        };
    }
}
```
